class Solution {
public:
    bool isPerfectSquare(int num) {
        if(num<1)
        return 0;
        for(  long int i=1;i*i<=num;i++)
        {
            if(i*i==num)
            return 1;
        }    
        return 0;
    }
};
